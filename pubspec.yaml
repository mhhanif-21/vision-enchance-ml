name: vision_enchance_ml
description: "Lumina Restore - AI Photo Restorer. On-device photo restoration using ONNX Runtime."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.0
  equatable: ^2.0.7
  flutter_onnxruntime: ^1.6.0
  image: ^4.5.3
  image_picker: ^1.1.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.5
  path: ^1.9.1
  photo_view: ^0.15.0
  shimmer: ^3.0.0
  google_fonts: ^6.2.1
  get_it: ^8.0.3
  go_router: ^14.8.1
  share_plus: ^10.1.4
  permission_handler: ^11.4.0
  uuid: ^4.5.1
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: any
  build_runner: any
  hive_generator: any

flutter:
  uses-material-design: true
  assets:
    - models/
